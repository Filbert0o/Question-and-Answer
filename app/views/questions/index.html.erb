<h1>Questions</h1>

<% @questions.each do |question| %>
  <ul>
    <li class="callout">
      Title: <%= link_to question.title, question_path(question) %><br />
      <%= question.description %>
    </li>
  </ul>
<% end %>


<button><%= link_to 'New Question', new_question_path %></button>
